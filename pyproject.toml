[project]
name = "leeds-agent-search"
version = "0.1.0"
description = "MCP Docker browser automation tools as Python functions and AWS Lambda handlers"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "aws-lambda-powertools>=3.24.0",
    "boto3>=1.35",
    "httpx>=0.27",
    "python-dotenv>=1.0",
    "requests>=2.32.5",
    "trafilatura>=2.0.0",
]
